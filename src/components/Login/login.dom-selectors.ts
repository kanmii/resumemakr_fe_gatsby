const prefix = "components-login";
export const domSubmitBtnId = `${prefix}-submit-btn`;
export const domEmailInputId = `${prefix}-email-input`
export const domPasswordInputId = `${prefix}-confirm-password-input`
export const domFormErrorId = `${prefix}-form-error`
