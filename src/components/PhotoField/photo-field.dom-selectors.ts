const prefix = "components-photo-field";
export const previewId = `${prefix}-previes`;
export const fileChooser = `${prefix}-file-chooser`;
export const deletePhotoId = `${prefix}-delete`;
export const stopPhotoDeleteId = `${prefix}-stop-photo-delete`;
